// la memoria en JS es manejada a traves del runtime
// este decide si soltar o no un espacio de memoria "allocated"
// usan el algoritmo Mark & Sweep -> funciona haciendo referencias
// a la memmoria

